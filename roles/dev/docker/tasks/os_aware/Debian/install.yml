---

- name: pick install script
  include: "install_using_{{ repo_install | ternary('repo', 'package') }}.yml"
  static: no
  when: ansible_distribution == 'Debian'
- name: post install
  include: '{{ role_path }}/tasks/os_aware/postinstall.yml'
  static: no
  when: ansible_distribution == 'Debian'
